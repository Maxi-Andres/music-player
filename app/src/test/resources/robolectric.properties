# Robolectric todavia no trae imagenes para API 37 (el compileSdk del proyecto),
# asi que los tests que lo usan corren contra API 34. Alcanza: lo que se prueba
# (el cursor de MediaStore) no cambia entre esas versiones.
sdk=34
